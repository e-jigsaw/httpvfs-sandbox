{
  "private": true,
  "name": "httpvfs-sandbox",
  "scripts": {
    "build:webpack": "webpack --mode=production",
    "copy:html": "cp index.html dist",
    "copy:sqlite": "cp example.sqlite3 dist",
    "deploy": "npm run build:webpack && npm run copy:html && npm run copy:sqlite"
  },
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.12.3",
    "prettier": "^2.3.2",
    "ts-loader": "^9.2.3",
    "typescript": "^4.3.5",
    "webpack": "^5.44.0",
    "webpack-cli": "^4.7.2"
  },
  "dependencies": {
    "sql.js-httpvfs": "^0.8.9"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
